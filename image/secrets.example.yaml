# Copy to config/secrets.yaml on the box (not committed).

# Clients open HA here (same front as BMS until WireGuard exists).
# Lab via BMS nginx: http://{slug}.ha.localhost:8080
# Later production: https://{slug}.ha.{company-domain}
ha_external_url: http://windows-lab.ha.localhost:8080

# Direct to this box on the LAN (Companion internal URL).
ha_internal_url: http://192.168.0.10:8123
