# Seeded to config/secrets.yaml on first boot (placeholders only — no lab/machine values).
# Deploy scripts or the owner should set real URLs for this box.

# Public / Companion external URL (set per deployment).
ha_external_url: http://homeassistant.local:8123

# Companion internal URL on the house LAN (set per deployment).
ha_internal_url: http://homeassistant.local:8123

# Long-lived Home Box token (GET). Prefer enabling sensory share in
# Company access — that auto-creates bms_ha_token here.
# bms_ha_token: replace-with-token

# Homeowner portal — manage which companies see sensory share.
# bms_portal_url: https://bms.example.com/portal
